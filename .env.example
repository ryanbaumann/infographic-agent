# Gemini API Key (required for generation)
# Get yours at: https://aistudio.google.com/apikey
# Leave empty to enter your key in the app's settings panel instead (stored in your browser only).
# WARNING: a key set here is inlined into dist/index.html at build time — never build a public artifact with a real key.
VITE_GEMINI_API_KEY=
